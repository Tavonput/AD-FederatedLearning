Add ClientProxy and ServerProxy classes. They will hold the actor handles and some meta data about the client.

Refactor the proxies to take forward *args and **kwargs.

Add eval to the new P2P setup

Run main experiment
